Dear <%= @user.username %>,
===============================

The ranking for watched repositories by your followings.

<% @watch_events.watched_ranking.each do |repo_name, events, repo| %>
* <%= repo_name %>: (<%= events.map {|event| event['actor']['login'] }.uniq.join(', ') %>) [<%= repo.stargazers_count %>]
  <%= repo.description %>
  <%= github_url(repo_name) %>

<% end %>

-- 
To stop these email go to setting page (<%= settings_email_url(host: Settings.host) %>)
and uncheck `Subscribe`.
