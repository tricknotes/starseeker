Dear <%= @user.username %>,
===============================

The starred repositories by your followings.

<% @ranking.each do |repo_name, events, repo| %>
* <%= repo_name %>: (<%= events.map {|event| event.actor_login }.uniq.join(', ') %>) [<%= repo.stargazers_count %>]<%= ' ★ Starred' if @starred_by_me.include?(repo_name) %>
  <%= repo.description %>
  <%= github_url(repo_name) %>

<% end %>

-- 
To stop these email go to setting page (<%= settings_email_url %>)
and uncheck `Subscribe`.
