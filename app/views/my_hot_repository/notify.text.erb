Dear <%= @user.username %>,
===============================

The ranking for watched repositories by your followings.

<% @watch_events.watched_ranking.each do |repo_name, events, repo| %>
* <%= repo_name %>: (<%= events.map {|event| event['actor']['login'] }.uniq.join(', ') %>) [<%= repo.watchers_count %>]
  <%= repo.description %>
  <%= github_url(repo_name) %>

<% end %>

-- 
To stop these email go to setting page (<%= Settings.base_url + settings_email_path %>)
and uncheck `Subscribe`.
