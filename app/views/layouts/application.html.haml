!!! html
%html
  %head
    %title= [@title, 'starseeker'].compact.join(' | ')
    = stylesheet_link_tag    'application', media: 'all'
    = javascript_include_tag 'application'
    = csrf_meta_tags
    = yield :head

  %body
    %header
      %h1
        = link_to 'starseeker', Settings.base_url
      %nav
        %ul
          - if logged_in?
            %li= link_to 'Hot repositories', hot_repositories_path
            %li= link_to 'dashboard', dashboard_path
            %li= link_to 'Logout', logout_path, method: :delete
          - else
            %li= link_to 'Sign in with GitHub', auth_at_provider_path(provider: :github)
      %p#notice= notice
      %p#alert= alert

    = yield :outside_content

    #content
      = yield

    %footer
      %span
        © 2012-2013 Ryunosuke SATO(#{ link_to '@tricknotes', github_url('tricknotes') }). All rights reserved.
