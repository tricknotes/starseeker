%h1 Root#index

Members:

%ul
- @users.each do |user|
  %li
    = link_to(image_tag(user.avatar_url, :size => '35x35'), github_url(user.username))

= link_to 'Sign in with GitHub', auth_at_provider_path(:provider => :github)
