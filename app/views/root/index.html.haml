.catch-phrase
  Watch your followings watching!

Members:

%section.members
  %ul
    - @users.each do |user|
      %li
        = link_to(image_tag(user.avatar_url, size: '35x35'), user_path(user.username))
