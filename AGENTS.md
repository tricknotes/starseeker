# AGENTS.md

このリポジトリで作業するエージェント向けのガイドです。アプリケーションの概要、セットアップ、Rake タスク、テストの実行方法は [README.md](README.md) を参照してください。

## やり取り

- ユーザーとのやり取りは日本語で行ってください。
- コミットメッセージ、PR タイトル、コード中のコメントは英語で書いてください。

## 開発環境と確認

- Ruby / Rails のバージョンは `Gemfile` を正とします。
- 変更後は、push する前に次を実行して通ることを確認してください。CI（`.github/workflows/`）でも同じものが実行されます。
  - `bundle exec rake`（RSpec）
  - `bundle exec brakeman --quiet --no-pager --exit-on-warn --exit-on-error`
- CI は `RUBYOPT=-W:deprecated` で動いています。deprecation 警告を新たに出さないでください。
- ローカルで確認できなかったこと（Ruby のバージョンが合わない、外部 API に到達できない、実際のメールクライアントで見ていない等）は、PR の説明にその旨を明記してください。

## コード

- 周囲のコードの書き方に合わせてください。
- コメントは、コードを読んでも分からない「なぜ」を説明するものだけを残してください。コードを読めば分かる「何を」は書かないでください。
- フレームワークが既定で行うことを設定で重ねて書かないでください。
- 振る舞いを変える変更、またはリファクタリングで既存テストが守っていない部分に触れる変更には、spec を追加してください。
- 回避策として依存を固定する場合（例: `gem 'json', '< 3'`）は、解除条件と関連する upstream の Issue / PR を `TODO` コメントで残してください。
- ローカルで生成されたファイル（`dump.rdb` などの Redis スナップショット、ログ、一時ファイル）をコミットしないでください。

## コミット

- 1 つのコミットは 1 つの論理的な変更にしてください。
- 件名は英語の命令形で、先頭を大文字にし、末尾にピリオドを付けません。`feat:` のような prefix は付けません。
  - 例: `Fix N+1 query in StarEvent.starred_ranking`, `Update Ruby from 4.0.6 to 4.0.7`
- 本文には、何を変えたかよりも、なぜ変えたか・何を検討して何を選んだかを書いてください。Issue を解決する場合は `Closes #<番号>` を含めてください。

## ブランチと PR

- `master` に直接 push せず、トピックブランチから PR を作成してください。
- 1 つの PR は 1 つの目的に絞ってください。目的の違う変更（リファクタリングと機能追加、別々のリファクタリング等）は別の PR に分けてください。
- PR はマージコミットで取り込まれます。PR のタイトルは主となるコミットの件名と揃えてください。
- PR の説明には、変更の規模に応じて次を書いてください。小さな変更では箇条書き数行で十分です。
  - **Problem / 背景**: なぜ変更が必要か
  - **Change**: 何を変えたか。あえて変えなかったこととその理由
  - **Verification / Test plan**: 実行したテスト・確認手順とその結果、確認できていないこと
  - **Notes**: スコープ外として残した課題など
- 外部の事実（リリース日、upstream の修正、互換性の情報など）に基づく判断には、その根拠となる URL を添えてください。

## 依存関係の更新

- GitHub Actions は Dependabot が毎週更新します（`.github/dependabot.yml`）。Actions はコミット SHA で固定し、行末にバージョンをコメントで残してください。
  - 例: `uses: actions/checkout@<sha> # v7.0.1`
- Ruby のバージョンを上げるときは、次のすべてを揃えて更新してください。必要なら `ruby/setup-ruby` も新しいバージョンに対応したものへ上げてください。
  - `Gemfile`
  - `Gemfile.lock`（`RUBY VERSION`）
  - `Dockerfile`
  - `.github/workflows/build.yml`
  - `.github/workflows/brakeman.yml`
- gem の更新で非互換が見つかった場合は、原因と対応（固定したバージョンとその解除条件）をコミットと PR に記載してください。
