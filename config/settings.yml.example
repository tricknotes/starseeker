defaults: &defaults
  github:
    client_id: <%= ENV['GITHUB_CLIENT_ID'] %>
    secret: <%= ENV['GITHUB_SECRET'] %>
  mail:
    sender: <%= ENV['MAIL_SENDER'] %>

development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults
